import styled from 'styled-components';

export const StyledTh = styled.th`
  cursor: pointer;
  font-size: 18px;
`;

export const ArrowIco = styled.span`
  position: relative;
  display: inline-block;
  min-width: 15px;
`;
